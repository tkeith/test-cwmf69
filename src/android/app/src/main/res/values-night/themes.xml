<?xml version="1.0" encoding="utf-8"?>
<!-- Material Design 3 dark theme configuration for Delayed Messaging System -->
<!-- Material3 version: 1.8.0 -->
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base application dark theme -->
    <style name="Theme.App" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary brand colors -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Secondary brand colors -->
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorSecondaryVariant">@color/color_secondary</item>
        <item name="colorOnSecondary">@android:color/white</item>

        <!-- Background & surface colors -->
        <item name="android:colorBackground">@color/color_background</item>
        <item name="colorSurface">@color/color_surface</item>
        <item name="colorOnSurface">@color/color_text_primary</item>
        <item name="colorOnSurfaceVariant">@color/color_text_secondary</item>

        <!-- Error colors -->
        <item name="colorError">@color/color_error</item>
        <item name="colorOnError">@android:color/white</item>

        <!-- Status bar and navigation -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>

        <!-- Text appearances -->
        <item name="textAppearanceHeadline1">@style/TextAppearance.App.Heading</item>
        <item name="textAppearanceHeadline2">@style/TextAppearance.App.Heading</item>
        <item name="textAppearanceBody1">@style/TextAppearance.App.Body</item>
        <item name="textAppearanceBody2">@style/TextAppearance.App.Body</item>

        <!-- Component styles -->
        <item name="materialButtonStyle">@style/Widget.App.Button.Primary</item>
        <item name="materialButtonOutlinedStyle">@style/Widget.App.Button.Secondary</item>
        <item name="materialCardViewStyle">@style/Widget.App.CardView</item>
        <item name="toolbarStyle">@style/Widget.App.Toolbar</item>

        <!-- Material elevation overlay -->
        <item name="elevationOverlayEnabled">true</item>
        <item name="elevationOverlayColor">@color/color_surface</item>

        <!-- Window background -->
        <item name="android:windowBackground">@color/color_background</item>
    </style>

    <!-- Splash screen theme for dark mode -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Splash screen background -->
        <item name="windowSplashScreenBackground">@color/color_background</item>
        
        <!-- Animated splash icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_animated</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/color_surface</item>
        
        <!-- Animation duration -->
        <item name="windowSplashScreenAnimationDuration">1000</item>
        
        <!-- Theme to transition to after splash -->
        <item name="postSplashScreenTheme">@style/Theme.App</item>
    </style>

</resources>